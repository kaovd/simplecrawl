1.0 - Inital commit
1.1 - Added js support and fixed minor issues, will now die if no results rather than write an empty file
1.2 - Fix an issue with using -c when not from a root url - now ensures robots and sitemap parsing is done via the base url
